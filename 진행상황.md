# 📋 학식 나우 — 진행 상황 (한눈에 보기)

> 실시간 학식 혼잡도 현황판 + '지금 주문' + **AI 추천** 모바일 웹
> 교내 해커톤 (12시간, 비전공자 3인) · 전부 목업 데이터로 동작

**🔗 배포 주소(공개): https://haksik-now.vercel.app** ← 폰에서 바로 열림

---

## ✅ 완료된 기능

| 화면 / 기능 | 상태 | 설명 |
|---|:---:|---|
| **현황판** (`/`) | ✅ | 식당 5곳 실시간 혼잡도·대기 인원·예상 시간, 게이지 바, 3초마다 갱신 |
| **AI 학식 추천** | ✅ | 지금 혼잡도·메뉴를 AI가 보고 한 곳/메뉴 추천 (FactChat 게이트웨이) |
| **식당 상세** (`/restaurant/:id`) | ✅ | 오늘 메뉴 + **시간대별 대기 꺾은선 차트**(그라데이션·피크 라벨) |
| **지금 주문** (`/order/:id`) | ✅ | 메뉴 담기(수량), 합계, 주문 → localStorage 저장 |
| **결제** (`/pay`) | ✅ | 결제수단 선택(카드·카카오·토스·계좌) → 결제(목업). 실제 돈·PG 없음 |
| **주문 완료** (`/order-complete`) | ✅ | 주문번호 + 가짜 QR + 주문 내역 |
| **장바구니** (`/cart`) | ✅ | 담은 메뉴 관리 |
| **주문 내역** (`/orders`) | ✅ | 상태 배지(접수→조리중→픽업 대기) |
| **하단 탭바** | ✅ | 홈 · 장바구니 · 주문내역 |
| **배포** | ✅ | Vercel 프로덕션, 공개 접속, AI 실동작 확인 |

## 🔧 남은 작업

| 항목 | 상태 | 비고 |
|---|:---:|---|
| 공용 컴포넌트 `Header.jsx` | ⬜ | 팀원3 담당 (없어도 동작엔 지장 없음) |
| 공용 컴포넌트 `CongestionBadge.jsx` | ⬜ | 팀원3 담당 (현재는 각 화면에서 직접 표시 중) |

---

## 🧩 기술 스택

- **프론트**: React + Vite, react-router-dom, 순수 CSS
- **AI**: FactChat 게이트웨이(OpenAI 호환), 모델 `claude-sonnet-4-6`
- **서버리스**: Vercel Functions (`/api/recommend`) — API 키는 서버에서만 사용(브라우저 노출 X)
- **배포**: Vercel · **데이터는 전부 목업**

## 🗺️ 화면 흐름

```
현황판(/) ──▶ 식당 상세(/restaurant/:id) ──▶ 지금 주문(/order/:id) ──▶ 결제(/pay) ──▶ 주문 완료(/order-complete)
   │                                                                  
   ├─▶ 장바구니(/cart)      └─▶ 주문 내역(/orders)                    
   └─▶ 🤖 AI 추천 (현황판 상단 카드)
```

---

## 🏆 평가 기준 대응

| 기준 (배점) | 우리 프로젝트에서 |
|---|---|
| **문제 정의·기획성** (20) | "지금 어디가 한가한가"를 실시간 혼잡도+대기시간으로 해결, 붐빌 때 '지금 주문'으로 우회 |
| **AI 활용 역량** (20) | ① 제품: AI 학식 추천(FactChat) · ② 과정: **전 과정 Claude Code로 개발** |
| **기술 구현 완성도** (30) | React+Vite 6개 화면, 서버리스 API 연동, 실배포·실동작 |
| **실용성·활용 가능성** (20) | 실제 캠퍼스 점심 시나리오 그대로, 폰에서 바로 사용 가능 |

---

## ⚙️ 개발/배포 메모

```bash
npm install        # 최초 1회
npm run dev        # 로컬 개발 (http://localhost:5173)
npm run build      # 프로덕션 빌드
```

- **AI 추천을 로컬에서 쓰려면**: 프로젝트 루트에 `.env.local` 만들고 `FACTCHAT_API_KEY=본인키` (`.env.example` 참고, git에 안 올라감)
- **배포 재반영**: `git push` 후 `vercel --prod` (환경변수는 Vercel에 이미 등록됨)
- ⚠️ **키 보안**: FactChat 키가 노출됐다면 시연 후 재발급(rotate) 권장

---

_최종 업데이트: 2026-08-06 · 최신 커밋 `456c750`_
